/**
 * Public Services
 * Services for public-facing features (no authentication required)
 * 
 * These services handle public data that doesn't require user authentication
 */

// Public services can be added here as needed
// Example: publicNewsService, publicCategoryService, etc.

module.exports = {
  // Public services will be exported here
};

